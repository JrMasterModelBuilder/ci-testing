'use strict';

describe('example', () => {
	it('linty', () => {
		expect(true).toBe(true)
	});

	it('pass', () => {
		expect(true).toBe(true);
	});

	it('fail', () => {
		expect(false).toBe(true);
	});
});
